/**
 * Canonical public fairness wording. Reuse on marketing surfaces.
 * Keep AWS / keeper / Postgres out of these one-breath lines.
 */

/** Primary one-liner for hero, footer, OG, Telegram whoPicks, docs Who picks. */
export const WHO_PICKS_ONE_LINER =
  "A person does not pick the winner. The public Closing Bell formula picks one wallet at random from the locked ticket list. Check any ring on /verify.";

/** Optional second breath where space allows. */
export const WHO_PICKS_SAME_MATH =
  "Same list + same formula -> same wallet.";

/** Short beat for strips and captions. */
export const FORMULA_PICKS_BEAT =
  "The public formula picks the wallet. Check any ring on /verify.";
